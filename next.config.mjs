/** @type {import('next').NextConfig} */
const nextConfig = {
    
        images: {
          domains: ['res.cloudinary.com'], // Add all necessary domains here
        },
      
};

export default nextConfig;
